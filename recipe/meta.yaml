{% set name = "vim" %}
{% set version = "9.1.0938" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/vim/vim/archive/v{{ version }}.tar.gz
  sha256: 556fc5db944273aae0d7b529ef744562e8391bd66a32f332a9856d5c719a9d1a
  patches:
    - 0001-fix-cross-compile-of-timer_create-without-lrt.patch

build:
  number: 0
  skip: true  # [win and python_impl == 'pypy']

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
    - make
  host:
    - python
    - perl     # [not win]
    - ncurses  # [not win]
    - gettext
    - xorg-libx11     # [linux]
    - xorg-libxt      # [linux]
  run:
    - python
    - perl     # [not win]
    - ncurses  # [not win]
    - gettext
    - xorg-libx11     # [linux]
    - xorg-libxt      # [linux]

test:
  commands:
    - vim --version
    - if not exist %LIBRARY_BIN%/vim.exe exit 1  # [win]
    - test -f $PREFIX/bin/vim                    # [not win]

about:
  home: https://www.vim.org/
  # vi(m) is licensed as charity-ware
  license: Vim
  license_file: runtime/doc/uganda.txt
  summary: Vim is a greatly improved version of the good old UNIX editor Vi
  dev_url: https://github.com/vim/vim
  doc_url: https://www.vim.org/docs.php

extra:
  recipe-maintainers:
    - mariusvniekerk
    - scopatz
    - asford
    - matthiasdiener
